set(RWC_CORE_DIR ${CMAKE_CURRENT_LIST_DIR})

file(GLOB_RECURSE RWC_CORE_HEADERS
    ${RWC_CORE_DIR}/*.h
    ${RWC_CORE_DIR}/*.hpp
)

file(GLOB_RECURSE RWC_CORE_SOURCES
    ${RWC_CORE_DIR}/*.cpp
)

target_sources(rwc PRIVATE ${RWC_CORE_HEADERS} ${RWC_CORE_SOURCES})