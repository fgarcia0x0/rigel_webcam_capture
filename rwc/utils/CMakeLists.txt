set(RWC_UTILS_DIR ${CMAKE_CURRENT_LIST_DIR})

file(GLOB_RECURSE RWC_UTILS_HEADERS
    ${RWC_UTILS_DIR}/*.h
    ${RWC_UTILS_DIR}/*.hpp
)

file(GLOB_RECURSE RWC_UTILS_SOURCES
    ${RWC_UTILS_DIR}/*.cpp
)

target_sources(rwc PRIVATE ${RWC_UTILS_HEADERS} ${RWC_UTILS_SOURCES})